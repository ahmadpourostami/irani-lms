<?php
/**
 * Irani LMS uninstall handler.
 *
 * Destructive data removal is intentionally disabled in the first release.
 * A future setting will allow administrators to explicitly remove LMS data.
 */

defined( 'WP_UNINSTALL_PLUGIN' ) || exit;
